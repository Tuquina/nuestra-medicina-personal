PROD_DOMAIN=example.com
DEV_DOMAIN=dev.example.com
# A real, reachable inbox — Let's Encrypt uses this only to warn about
# certificate problems (e.g. approaching expiry from a renewal failure),
# never anything public-facing.
ACME_EMAIL=you@example.com
